# macOS 工作舱系统 - 开发计划

## 第一天：项目初始化

### 1. 创建项目结构
```bash
cd projects
mkdir WorkPod
cd WorkPod

# 创建 Swift Package
swift package init \
  --type executable \
  --name WorkPod \
  --git-configuration none
```

### 2. 项目结构
```
WorkPod/
├── Sources/
│   └── WorkPod/
│       ├── main.swift
│       ├── Models/
│       │   ├── Cockpit.swift
│       │   ├── Member.swift
│       │   └── LayoutSnapshot.swift
│       ├── Managers/
│       │   ├── CockpitManager.swift
│       │   └── WindowManager.swift
│       ├── UI/
│       │   └── MainWindow.swift
│       └── Utils/
│           ├── Logger.swift
│           └── Keychain.swift
├── Package.swift
├── README.md
└── docs/
    └── technical-analysis.md
```

### 3. 基础代码模板

#### main.swift
```swift
import SwiftUI

@main
struct WorkPodApp: App {
    @StateObject private var cockpitManager = CockpitManager()
    
    var body: some Scene {
        WindowGroup {
            MainWindow()
                .environmentObject(cockpitManager)
        }
    }
}
```

#### Models/Cockpit.swift
```swift
import Foundation

struct Cockpit: Identifiable, Codable, Equatable {
    let id: String
    var name: String
    var icon: String?
    var members: [Member]
    var defaultMode: CockpitMode
    var createdAt: Date
    var updatedAt: Date
}

enum CockpitMode: String, Codable {
    case normal
    case immersive
    case pseudoFullscreen
}

struct Member: Identifiable, Codable, Equatable {
    let id: String
    var bundleId: String
    var name: String
    var launchConfig: LaunchConfig
    var layout: WindowLayout
    var isPrimary: Bool
}

struct LaunchConfig: Codable, Equatable {
    var urls: [String]?
    var directory: String?
    var profile: String?
}

struct WindowLayout: Codable, Equatable {
    var x: Double
    var y: Double
    var width: Double
    var height: Double
    var displayId: String?
}
```

#### Managers/CockpitManager.swift
```swift
import Foundation

class CockpitManager: ObservableObject {
    @Published var cockpits: [Cockpit] = []
    @Published var activeCockpitId: String?
    
    private let storageKey = "WorkPodCockpits"
    
    init() {
        loadCockpits()
    }
    
    func loadCockpits() {
        // 从 UserDefaults 加载配置
    }
    
    func saveCockpits() {
        // 保存到 UserDefaults
    }
    
    func addCockpit(_ cockpit: Cockpit) {
        // 添加工作舱
    }
    
    func removeCockpit(id: String) {
        // 删除工作舱
    }
    
    func activateCockpit(id: String) {
        // 激活工作舱
    }
}
```

#### Managers/WindowManager.swift
```swift
import Foundation
import Cocoa

class WindowManager: ObservableObject {
    @Published var windowList: [WindowInfo] = []
    
    func launchApp(bundleId: String) {
        // 启动应用
    }
    
    func getAllWindows() -> [WindowInfo] {
        // 获取所有窗口
    }
    
    func restoreLayout(layout: WindowLayout, for memberId: String) {
        // 恢复窗口布局
    }
}

struct WindowInfo: Identifiable {
    let id: Int
    let title: String
    let bounds: CGRect
    let ownerId: Int
    let bundleId: String?
}
```

---

## 第二天：核心功能开发

### 1. 应用启动模块
- 实现应用启动逻辑
- 添加应用状态监控
- 处理应用已运行情况

### 2. 窗口检测模块
- 实现窗口列表获取
- 解析窗口信息
- 保存/恢复窗口布局

---

## 第三天：UI 开发

### 1. 主窗口
- 工作舱列表
- 快速切换按钮

### 2. 工作舱编辑
- 添加/编辑成员
- 配置布局

---

## 第四天：集成测试

### 1. 单元测试
- 应用启动测试
- 窗口检测测试

### 2. 集成测试
- 完整工作舱流程
- 跨舱切换测试

---

## 第五天：优化与文档

### 1. 性能优化
- 窗口检测性能
- UI 响应速度

### 2. 文档
- 用户手册
- 开发文档

---

## 实施检查清单

- [ ] 项目初始化完成
- [ ] 数据模型定义完成
- [ ] 应用启动模块完成
- [ ] 窗口检测模块完成
- [ ] 工作舱管理完成
- [ ] UI 开发完成
- [ ] 集成测试完成
- [ ] 文档完成

---

**目标：5 天完成 MVP 版本！**
